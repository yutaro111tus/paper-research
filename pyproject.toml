[project]
name = "paper-research"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yutaro", email = "7424701@ed.tus.ac.jp" }
]
requires-python = ">=3.12"
dependencies = ["python-dotenv>=1.0.1", "openai>=1.0.0"]

[project.scripts]
paper-research = "paper_research:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
